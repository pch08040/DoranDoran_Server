/**
 * 게시글이 살아있는 시간. (기획서: 게시물 2일 뒤 자동삭제)
 *
 * 와글와글은 '지금 이 순간'을 나누는 곳이라 글이 오래 남으면 성격이 달라진다.
 * 2일이 지나면 피드에서 안 보이고, 매시 정각에 도는 청소가 실제로 지운다.
 */
export const POST_LIFETIME_MS = 2 * 24 * 60 * 60 * 1000;

/** 글자 수 상한. (기획서 FE-Waggle-006: 최대 20자) */
export const POST_MAX_LENGTH = 20;
